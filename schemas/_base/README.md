# schemas/_base

Reserved for shared base schema fragments — common field definitions that subject-specific schemas can reference. Currently empty; add base YAML fragments here as the schema system evolves.
